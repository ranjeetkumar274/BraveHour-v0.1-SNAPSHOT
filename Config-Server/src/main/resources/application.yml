spring:
  application:
    name: Config-Server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ranjeetkumar274/configuration-properties
          default-label: main
          search-paths: config
          clone-on-start: true
server:
  port: 8888